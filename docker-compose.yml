name: slotsone-local

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: slotsone
      POSTGRES_USER: slotsone
      POSTGRES_PASSWORD: slotsone
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U slotsone"]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - slotsone

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: slotsone-backend-local
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3001
      CORS_ORIGINS: ${CORS_ORIGINS:-*}
      JWT_ALLOWED_ALGS: RS256
      JWT_PUBLIC_KEY: ${JWT_PUBLIC_KEY}
      JWT_ISSUER: ${JWT_ISSUER:-slotsone-dev}
      JWT_AUDIENCE: ${JWT_AUDIENCE:-slotsone-client}
      DATABASE_URL: postgres://slotsone:slotsone@postgres:5432/slotsone
    ports:
      - "3001:3001"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:3001/health >/dev/null || exit 1"]
      interval: 20s
      timeout: 3s
      retries: 5
      start_period: 20s
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - slotsone

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: slotsone-frontend-local
    restart: unless-stopped
    depends_on:
      backend:
        condition: service_healthy
    ports:
      - "8080:80"
    networks:
      - slotsone

  postgres-admin:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: slotsone_admin
      POSTGRES_USER: slotsone_admin
      POSTGRES_PASSWORD: slotsone_admin
    ports:
      - "5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U slotsone_admin"]
      interval: 10s
      timeout: 5s
      retries: 5
    volumes:
      - postgres_admin_data:/var/lib/postgresql/data
    networks:
      - slotsone

  admin:
    build:
      context: ./admin
      dockerfile: Dockerfile
    container_name: slotsone-admin-local
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3002
      ADMIN_DATABASE_URL: postgres://slotsone_admin:slotsone_admin@postgres-admin:5432/slotsone_admin
      NEXTAUTH_URL: http://localhost:3002/admin
      NEXTAUTH_SECRET: dev-secret-change-in-production
    ports:
      - "3002:3002"
    depends_on:
      postgres-admin:
        condition: service_healthy
    networks:
      - slotsone

networks:
  slotsone:
    driver: bridge

volumes:
  postgres_data:
  postgres_admin_data:
